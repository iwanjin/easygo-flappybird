# 🎯 1분마다 모니터링 시스템

**목적**: 3팀 병렬 개발 중 이상 징후를 빠르게 감지하고 수정  
**주기**: 1분마다 (자동)  
**방식**: 마스터 터미널에서 모니터링, 문제 발생 시 즉시 일시정지

---

## 📊 모니터링 대시보드

### Phase 1 (09:30 ~ 12:00)

#### ✅ 체크 1분 (09:31)
**목표**: 3팀 모두 파일 생성 시작

```
Team A: game.js, renderer.js 생성 시작했는가?
  → 확인: ls -la js/game.js js/renderer.js
  → 예상: 파일 존재 (내용은 비어있어도 OK)

Team B: index.html, style.css 생성 시작했는가?
  → 확인: ls -la index.html css/style.css
  → 예상: 파일 존재

Team C: storage.js, sound.js 생성 시작했는가?
  → 확인: ls -la js/storage.js js/sound.js
  → 예상: 파일 존재

✅ 통과 조건: 3팀 모두 파일 존재
🔴 실패 조건: 파일 누락 → 팀에 상태 확인 요청
```

---

#### ✅ 체크 2분 (09:32)
**목표**: Canvas 초기화 확인

```
# 마스터 터미널에서 테스트
python -m http.server 8000  # 이미 실행 중일 수 있음

# 브라우저 F12 Console에서 (또는 curl 확인)
curl -s http://localhost:8000 | grep -c '<canvas'  # 1 이상이어야 함

✅ 통과 조건: Canvas 요소 존재
🔴 실패 조건: Canvas 없음 → Team B에 index.html 확인 요청
```

---

#### ✅ 체크 3분 (09:33)
**목표**: Script 로드 순서 확인

```javascript
// 브라우저 F12 Console에서 입력
typeof Storage === 'object'   // true여야 함 (C팀)
typeof Sound === 'object'     // true여야 함 (C팀)
typeof Renderer === 'object'  // true여야 함 (A팀)
typeof Game === 'object'      // true여야 함 (A팀)

✅ 통과 조건: 모두 'object' 반환
🔴 실패 조건: undefined → script 순서 오류
  해결: index.html의 <script> 태그 순서 재확인
```

---

#### ✅ 체크 4분 (09:34)
**목표**: Game 모듈 초기화 가능한가?

```javascript
// 브라우저 F12 Console에서
Game.init('medium', 0)   // 에러 없어야 함
Game.start()             // 에러 없어야 함
console.log(Game.getState())  // 객체 반환해야 함

✅ 통과 조건: 모든 함수 호출 성공
🔴 실패 조건: TypeError → Team A에 game.js 문법 확인 요청
```

---

#### ✅ 체크 5분 (09:35)
**목표**: Console 에러 0개 확인

```
F12 → Console 탭 → 빨간 X 개수 확인

✅ 통과 조건: 에러 0개
🟡 주의 조건: 경고(노란색) 1-2개는 OK
🔴 실패 조건: 에러(빨간색) 1개 이상 → 즉시 일시정지 후 디버깅
  해결: 에러 메시지 읽고 팀에 코드 확인 요청
```

---

### Phase 2 (13:00 ~ 16:00)

#### ✅ 체크 1분 (13:01)
**목표**: 게임 플레이 가능한가?

```javascript
// 게임 시작
Game.init('medium', 0)
Game.start()

// 새가 떨어지는가? (중력 확인)
// Canvas에 파란 배경 + 파란 새가 보여야 함

✅ 통과 조건: 새가 천천히 떨어짐
🔴 실패 조건: 새가 안 움직임 → Team A에 updatePhysics 확인 요청
```

---

#### ✅ 체크 2분 (13:02)
**목표**: 점프 동작 확인

```javascript
// 새가 떨어지는 상태에서
Game.jump()  // 또는 클릭

// 새가 올라가는가?
✅ 통과 조건: 새가 위로 올라갔다가 떨어짐
🔴 실패 조건: 점프 안 됨 → Team A의 jump() 함수 확인
```

---

#### ✅ 체크 3분 (13:03)
**목표**: 파이프 생성 & 이동 확인

```
게임을 몇 초 플레이한 후 Canvas에 회색 파이프가 보이는가?

✅ 통과 조건: 파이프 2개 이상 보임
🔴 실패 조건: 파이프 없음 → Team A의 updatePipes() 확인
```

---

#### ✅ 체크 4분 (13:04)
**목표**: 충돌 감지 확인

```javascript
// 게임을 계속 플레이 후 파이프와 부딪히면
// game:over 이벤트가 발행되고 게임이 멈춰야 함

✅ 통과 조건: 충돌 후 게임 멈춤
🔴 실패 조건: 게임이 계속 실행 → Team A의 checkCollision() 확인
```

---

#### ✅ 체크 5분 (13:05)
**목표**: 점수 계산 정확한가?

```javascript
Game.getState().score  // 현재 점수

// 파이프를 통과할 때마다 1씩 증가하는가?
// (중복 없이)

✅ 통과 조건: 점수가 정확히 증가
🟡 주의: 점수가 2씩 증가하면 passed 플래그 누락 가능
🔴 실패 조건: 점수가 증가 안 함 → Team A의 updateScore() 확인
```

---

#### ✅ 체크 6분 (13:06)
**목표**: UI 화면 전환 확인

```javascript
// B팀이 ui.js를 완성했는가?
UI.showScreen('start')   // 시작 화면 보임?
UI.showScreen('game')    // 게임 화면 보임?
UI.showScreen('gameover') // 게임오버 화면 보임?

✅ 통과 조건: 3개 화면 모두 전환됨
🔴 실패 조건: 화면이 안 바뀜 → Team B의 ui.js 확인
```

---

#### ✅ 체크 7분 (13:07)
**목표**: 점수 업데이트 정상 표시되는가?

```
게임 플레이 중에 Canvas 위의 점수 표시가 실시간으로 변하는가?

✅ 통과 조건: 점수가 실시간 업데이트됨
🔴 실패 조건: 점수가 안 업데이트됨 → Team B의 updateScore() 확인
```

---

#### ✅ 체크 8분 (13:08)
**목표**: 신기록 저장 확인

```javascript
// 게임을 플레이하고 게임오버
// 그 점수가 localStorage에 저장되었는가?

Storage.getBestScore()  // 게임 점수와 일치해야 함

✅ 통과 조건: Storage에 점수 저장됨
🔴 실패 조건: 점수가 안 저장됨 → Team C의 storage.js 확인
```

---

#### ✅ 체크 9분 (13:09)
**목표**: 사운드 재생 확인

```javascript
Sound.isMuted()  // false여야 함

// 게임 플레이 중 소리가 나는가?
// - 점프할 때: 소리
// - 파이프 통과: 소리
// - 충돌할 때: 소리

✅ 통과 조건: 효과음이 나타남
🔴 실패 조건: 소리 안 남 → Team C의 sound.js + 이벤트 리스너 확인
```

---

#### ✅ 체크 10분 (13:10)
**목표**: Console 에러 0개 확인

```
F12 → Console 탭 → 빨간 X 개수 확인

✅ 통과 조건: 에러 0개
🔴 실패 조건: 에러 1개 이상 → 즉시 일시정지 후 디버깅
```

---

### Phase 3 (16:00 ~ 17:00)

#### ✅ 통합 테스트
**목표**: 게임이 완전히 작동하는가?

```
1. 시작 화면에서 게임 시작
2. 캐릭터/난이도 선택
3. 게임 플레이
4. 파이프 통과 (점수 증가, 소리 재생)
5. 충돌 (게임오버 화면)
6. 신기록 확인 (localStorage 저장)
7. 다시 시작

✅ 통과 조건: 모든 과정이 부드럽게 진행
🔴 실패 조건: 어느 단계에서든 오류 → 즉시 일시정지 후 팀별 확인
```

---

## 🚨 문제 대응 절차

### 1단계: 문제 감지 (1분 체크에서 발견)

```
❌ "Team A: game.js 파일이 없다"
↓
2단계: 즉시 일시정지
```

---

### 2단계: 일시정지 (다른 팀은 계속 개발)

```
[마스터 메시지]
"Team A, 일시정지. game.js 파일 생성 여부 확인 요청."

[Team A]
"파일을 생성했습니다. 경로 확인 중..."
```

---

### 3단계: 문제 진단 (10분 이내)

```
가능한 원인:
1. 파일이 잘못된 디렉토리에 생성됨
2. 파일명 오타 (예: game.JS)
3. 파일은 있지만 코드가 빈 상태
4. 스크립트 로드 순서 오류
```

---

### 4단계: 빠른 수정 (5분 이내)

```
해결책:
1. 파일 위치 재확인
2. 코드 복사/붙여넣기 다시
3. script 태그 순서 재정렬
4. 브라우저 새로고침
```

---

### 5단계: 재검증 (1분)

```javascript
// 다시 체크
typeof Game === 'object'  // true?
Game.init('medium', 0)    // 에러 없음?

✅ 통과 → 팀 재개 지시
🔴 실패 → 다시 3단계로
```

---

## 📋 모니터링 체크리스트 (자동화 가능)

각 체크를 스크립트로 자동화할 수 있습니다:

```bash
#!/bin/bash
# check.sh - 1분마다 실행

# 1. 파일 존재 확인
test -f js/game.js && echo "✅ game.js" || echo "❌ game.js"
test -f js/storage.js && echo "✅ storage.js" || echo "❌ storage.js"
test -f js/sound.js && echo "✅ sound.js" || echo "❌ sound.js"

# 2. HTTP 서버 실행 중 확인
curl -s http://localhost:8000 > /dev/null && echo "✅ Server" || echo "❌ Server"

# 3. 콘솔 테스트 (브라우저가 필요하므로 수동)
echo "🔍 수동 확인 필요:"
echo "  - typeof Game"
echo "  - typeof Storage"
echo "  - typeof Sound"
```

---

## 🎯 모니터링 목표

| 시점 | 체크 항목 | 예상 상태 |
|---|---|---|
| **09:35** (Phase 1 시작 5분 후) | 파일 생성, Script 로드 | ✅ 에러 0개 |
| **11:00** (Phase 1 중반) | Canvas 렌더링 | ✅ 파란 배경 보임 |
| **12:00** (Phase 1 종료) | 게임 플레이 가능 | ✅ 점수 계산 정확 |
| **13:10** (Phase 2 시작 10분 후) | UI 전환 정상 | ✅ 화면 3개 전환 가능 |
| **15:00** (Phase 2 중반) | 사운드, 신기록 저장 | ✅ 모두 작동 |
| **17:00** (전체 완료) | 통합 테스트 | ✅ 게임 완벽 작동 |

---

## 🔧 마스터 터미널 명령어

```bash
# 웹 서버 시작
python -m http.server 8000

# 파일 상태 확인 (매 분마다)
watch -n 1 'ls -la js/'

# 콘솔 에러 모니터링 (자동 새로고침)
# F12 → Console 탭에서 직접 관찰
```

---

## 📞 응급 상황 대응

### 상황 1: 게임이 로드되지 않음
```
1. F12 Console 열기
2. 빨간 에러 메시지 읽기
3. 마스터가 팀에 공유
4. 문제 팀이 원인 파악
5. 5분 내 수정 → 재검증
```

### 상황 2: 한 팀이 오래 멈춰있음
```
1. 팀에 상태 메시지 요청
2. 문제가 막힐 가능성이 있으면
   → 해당 팀만 작업 일시정지
   → 다른 팀은 독립적 부분 계속 진행
3. 마스터가 문제 팀 지원
```

### 상황 3: 두 팀의 인터페이스 불일치
```
예: UI.updateScore vs UI.updateScoreDisplay
1. 공유규약(00-work-methodology.md) 확인
2. 구현 코드가 규약과 일치하지 않으면
   → 규약을 따르도록 코드 수정
3. 규약이 잘못되면
   → 규약 업데이트 후 모든 팀 알림
```

---

## 🎉 성공 기준

### Phase 1 (12:00)
```
✅ Game.init() 호출 가능
✅ Game.start() 호출 가능
✅ Game.jump() 동작
✅ Canvas에 게임 렌더링
✅ Console 에러 0개
✅ fps > 50
```

### Phase 2 (16:00)
```
✅ 게임 플레이 가능 (충돌까지)
✅ UI 화면 전환 정상
✅ 점수 계산 정확
✅ 신기록 저장
✅ 효과음 재생
✅ 모바일 375px에서 정상 작동
```

### Phase 3 (17:00)
```
✅ 게임 완벽 작동
✅ 모든 기능 통합
✅ Console 에러 0개
✅ 배포 준비 완료
```

---

**모니터링 시작**: 09:30 (병렬 개발 시작 시)  
**모니터링 빈도**: 1분마다 자동  
**마스터**: 진행 상황 기록 & 문제 감지 시 즉시 대응

준비 완료! 🚀
